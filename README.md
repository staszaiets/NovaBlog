## 🛠 Технічний стек

- **Core:** [Next.js 15](https://nextjs.org/) (App Router), React 18, TypeScript.
- **State Management:** Redux Toolkit, React-Redux.
- **Backend & DB:** Firebase (Firestore).
- **Styling:** SCSS Modules, Custom Fonts (Source Sans Pro).
- **Forms & Validation:** React Hook Form, Zod.
- **Code Quality:** ESLint, Prettier, Husky, Lint-staged.

## 📂 Структура проєкту

Проєкт організовано за гібридною структурою, що поєднує маршрутизацію Next.js та Feature-Sliced підхід для бізнес-логіки:

```text
├── app/                  # Next.js App Router (сторінки та лейаути)
│   ├── (root)/           # Основна група маршрутів
│   │   ├── blog/         # Сторінка блогу (список та деталі)
│   │   └── page.tsx      # Головна сторінка
│   ├── layout.tsx        # Root Layout (Redux Provider, Fonts)
│   └── globals.scss      # Глобальні стилі
├── src/
│   ├── features/         # Бізнес-логіка (Slices, Components, Thunks)
│   │   ├── posts/        # Управління постами
│   │   ├── comments/     # Система коментарів
│   │   └── filters/      # Логіка фільтрації
│   ├── hooks/            # Типізовані хуки (useAppDispatch, useAppSelector)
│   ├── lib/              # Конфігурація Firebase
│   ├── store/            # Redux Store setup
│   └── types/            # TypeScript інтерфейси
└── public/               # Статичні файли
```

## 🚀 Запуск проєкту

### 1\. Попередні вимоги

Переконайтеся, що у вас встановлено **Node.js** (версія 18 або новіша).

### 2\. Встановлення залежностей

Встановіть необхідні пакети (це також автоматично налаштує `husky`):

```bash
npm install
# або
yarn install
```

### 3\. Налаштування Firebase

На даний момент конфігурація Firebase знаходиться у файлі `src/lib/firebase.ts`. Переконайтеся, що проєкт `novablog-b0b04` доступний та налаштований у консолі Firebase.

### 4\. Запуск локального сервера

Для запуску в режимі розробки виконайте:

```bash
npm run dev
```

Застосунок буде доступний за адресою [http://localhost:3000](https://www.google.com/search?q=http://localhost:3000).

## 📜 Доступні скрипти

У файлі `package.json` доступні наступні команди:

- `npm run dev` — Запуск режиму розробки.
- `npm run build` — Створення оптимізованого білда для продакшну.
- `npm run start` — Запуск продакшн-сервера.
- `npm run lint` — Перевірка коду за допомогою ESLint.
- `npm run type-check` — Перевірка типів TypeScript без компіляції.
- `npm run format` — Форматування коду через Prettier.

## ✨ Основні можливості

- **Перегляд постів:** Динамічний список статей з пагінацією/фільтрацією.
- **Деталі поста:** Окрема сторінка для кожної статті (slug-based routing) з розрахунком часу читання/page.tsx].
- **Створення постів:** Модальне вікно для створення нових записів, стан якого керується через URL (`?create=1`).
- **Коментарі:** Можливість додавати та переглядати коментарі до статей.
